import React from 'react';
import useCartStore from '../../store/CartStore';

const Product = ({ item }) => {
  console.log(item);
  return (
    <div>
      
    </div>
  );
};

export default Product;
